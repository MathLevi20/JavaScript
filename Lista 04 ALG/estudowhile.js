const prompt = require('prompt-sync')()
console.log("Bem vindo!!")

function main() {
    const nome = prompt("qual o seu nome:")
    // Trabalho anterior
    let atual = 0
    const alvo = 21
    const passo = 2
    
    while (atual < alvo){ // condição de parada/continuidade   
    //trabalho
        console.log(`${atual}:Seu nome é ${nome}`)
        //convergencia de dados
    atual = atual +passo}
}

main()